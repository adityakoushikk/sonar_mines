# @package _global_
# B4: YOLO26 P2 architecture with compatible COCO weights.

defaults:
  - override /init: yolo26_p2
  - override /augmentation: all   # TODO: confirm aug setting for init-ablation suite

tags: ["sonar", "yolo26", "exp:b4", "init:yolo26_p2"]
