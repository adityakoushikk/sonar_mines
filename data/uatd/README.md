# UATD — Underwater Acoustic Target Detection dataset

This directory is a placeholder for the UATD dataset used to pretrain the
**B5** initialization (`configs/init/sonar_uatd.yaml`).

Same modality (SSS) as Santos but a different scene/object distribution.

Expected on-disk layout (TODO: confirm once data is staged):

```
data/uatd/
├── images/
└── labels/
```
