# @package _global_
# B1: random init, baseline augmentation. Init-ablation lower bound.

defaults:
  - override /init: random
  - override /augmentation: none   # TODO: confirm aug setting for init-ablation suite

tags: ["sonar", "yolov8", "exp:b1", "init:random"]
