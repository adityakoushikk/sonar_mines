# Cross-year split of the Santos 2024 dataset, used for Experiment 3
# (generalization across collection years).

name: "santos_cross_year"
root: ""                        # TODO: absolute path to data/santos/ root
images_dir: ""                  # TODO: subdir with .jpg/.png files
labels_dir: ""                  # TODO: subdir with YOLO-format .txt files
class_names:                    # TODO: order must match label class indices
  - "MILCO"
  - "NOMBO"

split:
  kind: "cross_year"
  train_years: []               # TODO: list of years to use for train+val (e.g. [2015, 2017])
  test_years: []                # TODO: held-out year(s) (e.g. [2021])
  val_frac_of_train: 0.0        # TODO: fraction of train_years to hold out for validation
  seed: ${seed}

subsample_n: null               # TODO: int or null
