# @package _global_
# B2: COCO backbone/neck with randomly initialized detector weights.

defaults:
  - override /init: random_detector
  - override /augmentation: all

tags: ["sonar", "yolov8", "exp:b2", "init:random_detector", "aug:all"]
