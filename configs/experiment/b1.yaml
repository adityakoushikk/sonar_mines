# @package _global_
# B1: fully random YOLOv8 init with the all-augmentation condition.

defaults:
  - override /init: random
  - override /augmentation: all

tags: ["sonar", "yolov8", "exp:b1", "init:random", "aug:all"]
