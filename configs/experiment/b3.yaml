# @package _global_
# B3: full COCO-pretrained YOLO26 checkpoint init.

defaults:
  - override /init: yolo26_coco
  - override /augmentation: all   # TODO: confirm aug setting for init-ablation suite

tags: ["sonar", "yolo26", "exp:b3", "init:yolo26_coco"]
